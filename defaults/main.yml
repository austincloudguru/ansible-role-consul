---
# Consul group
consul_group: "consul"
consul_gid: "10010"

# Consul user
consul_user: "consul"
consul_uid: "10010"

# Consul Application
consul_version: "1.8.4"

# Consul log level
consul_loglevel: "INFO"

# Determines if you should start consul (for ami images)
consul_start: true
